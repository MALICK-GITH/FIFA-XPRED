'use client';
import {useState} from 'react';import Link from 'next/link';export default function IA(){const[q,setQ]=useState(''),[a,setA]=useState(''),[loading,setLoading]=useState(false);async function ask(){setLoading(true);try{const j=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:q,model:'deepseek-r1'})}).then(r=>r.json());setA(j.content||j.error)}finally{setLoading(false)}}return <main className='shell'><header className='top'><div><div className='brand'>SOLITAIRE HACK 🇨🇮</div><div className='tag'>Il est temps de voir grand</div></div><nav className='nav'><Link href='/'>Matchs</Link><Link href='/coupon'>Coupon IA</Link><Link href='/suivi'>Suivi</Link><Link href='/ia'>IA</Link></nav></header><section className='hero'><div className='tag'>DEEPSEEK R1</div><h1>Le cerveau du système.</h1><p className='muted'>L'IA reçoit le contexte du système et l'historique disponible pour expliquer les données. Elle ne remplace pas le moteur de prédiction.</p></section><textarea className='input' style={{width:'100%',minHeight:150}} value={q} onChange={e=>setQ(e.target.value)} placeholder='Pose une question sur les matchs, probabilités ou performances…'/><div className='controls'><button className='btn' onClick={ask} disabled={loading}>{loading?'Analyse…':'Interroger DeepSeek R1'}</button></div>{a&&<article className='card' style={{whiteSpace:'pre-wrap'}}>{a}</article>}</main>}
