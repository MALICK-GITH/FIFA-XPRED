import {NextRequest,NextResponse} from 'next/server';
export async function POST(req:NextRequest){try{const {prompt}=await req.json();const key=process.env.AI_API_KEY;if(!key)throw new Error('AI_API_KEY manquante');const r=await fetch('https://aimodelapi.onrender.com/v1/images/generations',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${key}`},body:JSON.stringify({model:'image-gen',prompt:`Luxury football prediction card, SOLITAIRE HACK 🇨🇮, slogan "Il est temps de voir grand", dark elegant premium UI, no bookmaker logo, no guaranteed-win claim. ${prompt}`,n:1,size:'1024x1024'})});const j=await r.json();if(!r.ok)throw new Error(JSON.stringify(j));return NextResponse.json({ok:true,url:j?.data?.[0]?.url})}catch(e:any){return NextResponse.json({ok:false,error:e.message},{status:500})}}
